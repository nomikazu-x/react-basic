import {useState} from "react";
import TextInput from "./TextInput";

const Article = (props) => {
    return (
        <div>
           <TextInput />
        </div>
    );
};

export default Article;